
import React from 'react';
import AppLayout from '@/components/layout/AppLayout';

const Dashboard = () => {
  return (
    <AppLayout>
      {/* The AppLayout already includes BoardHeader, BoardToolbar and KanbanBoard */}
    </AppLayout>
  );
};

export default Dashboard;
